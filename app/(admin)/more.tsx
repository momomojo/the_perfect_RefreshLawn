import React from 'react';
import { View } from 'react-native';

// This is a placeholder route that should never render
// The "More" tab opens a drawer instead via tabPress listener
export default function MoreScreen() {
  return null;
}
